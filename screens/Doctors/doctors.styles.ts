import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    borderTopLeftRadius: 16,
    borderTopRightRadius: 16,
    paddingTop: 24,
    backgroundColor: "white",
  },
  chipContainer: { height: 36 },
  tabContainer: { marginTop: 18 },
});
